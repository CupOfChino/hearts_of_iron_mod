GER_new_third_empire_has_war_categories = {
    GER_new_third_empire_has_war_active = { #世界大战自动任务

		icon = GFX_decision_generic_army_support

		days_mission_timeout = 365

		selectable_mission = no

		allowed = {
			
		}

		available = { #完成条件
            if = {
                limit = {
                    is_in_faction_with = GER
                }
                has_global_flag = the_ger_win_flag
            }
            if = {
                limit = {
                    is_in_faction_with = ENG
                }
                has_global_flag = the_eng_win_flag
            }
		}
		activation = {  #启用      
            is_debug = yes    #临时锁定
			AND = {
				OR = {
					is_in_faction_with = ENG
					is_in_faction_with = GER
				}
                has_global_flag = the_world_war_tow_flag
                NOT = {
                    has_global_flag = the_ger_win_flag
                }
                NOT = {
                    has_global_flag = the_eng_win_flag
                }
                NOT = {
                    has_global_flag = the_nuclear_ger_flag
                }
                NOT = {
                    has_global_flag = the_nuclear_eng_flag
                }
                is_subject = no
                OR = {
                    is_major = yes
                    is_ai = no
                    tag = GER
                    tag = ENG
                    tag = FRA
                    tag = HUN
                    tag = SOV
                    tag = USA
                    tag = JAP
                    tag = CHI
                }  
			}
		}    
		

		is_good = yes                      

		timeout_effect = { #二战没结束
			
			activate_mission = GER_new_third_empire_has_war_active
		}       
		complete_effect = {  #胜利效果
			
		}
		cancel_trigger = { #战败（二战结束了）
			if = {
                limit = {
                    is_in_faction_with = GER
                }
                has_global_flag = the_eng_win_flag
            }
            if = {
                limit = {
                    is_in_faction_with = ENG
                }
                has_global_flag = the_ger_win_flag
            }
		}
		cancel_effect = {
			
		}
	}


}